#!/usr/bin/zsh

# Add this to your ~/.zshrc:
# # Keep zsh open when run with eval:
# # zsh -is eval 'shell command'
# if [[ $1 == eval ]]; then
#   "$@"
#   set --
# fi
xfce4-terminal -e "zsh -lis eval 'shotgun config.ru'"
xfce4-terminal -e "zsh -lis eval 'bundle exec guard'"
xfce4-terminal -e "zsh -l"
